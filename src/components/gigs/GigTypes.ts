export type GigProps = {
    id: number,
    imgUrl?: string,
    name: string,
    venue?: string,
    artist?: string,
    description?: string;
    date?: string,
    start?: string,
    isPublic?: boolean
}