export type GigProps = {
    id: number,
    image?: string,
    name: string,
    venue?: string,
    artists?: string,
    description?: string,
    date?: string,
    time?: string,
    fee?: string,
    isPublic?: boolean
}